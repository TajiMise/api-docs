import Navbar from '../components/Navbar';
import '../styles/Home.scss';

const Home = () => {
    return (
        <div className="home">
            <Navbar currentPage="home" />
        </div>
    );
};

export default Home;
